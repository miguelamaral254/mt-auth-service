spring:
  application:
    name: login-auth-api
  datasource:
    url: jdbc:mysql://localhost:3306/mt_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 32412294
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

api:
  security:
    token:
      secret: key-3241-key
